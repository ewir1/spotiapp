import { DomseguidoresPipe } from './domseguidores.pipe';

describe('DomseguidoresPipe', () => {
  it('create an instance', () => {
    const pipe = new DomseguidoresPipe();
    expect(pipe).toBeTruthy();
  });
});
